{{#each user in model}}
  <div class='user'>
    <p>{{user.id}}</p>
    <p>{{user.twitter_name}}</p>
    <p>{{user.insta_name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.groups}}</p>
  </div>
{{/each}}
