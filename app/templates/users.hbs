<div class="row">
  <div class="small-4 columns small-offset-4">

    {{#each user in model}}
      <div class='user'>
        <p>{{user.id}}</p>
        <p>{{user.twitter_name}}</p>
        <p>{{user.insta_name}}</p>
        <p>{{user.email}}</p>
      </div>
    {{/each}}

  </div>
</div>
